# Chronicle Configuration
# Copy this file to .env and modify as needed
# Load with: export $(cat .env | xargs)

# Transport backend: "otp" (default) or "rabbitmq"
CHRONICLE_TRANSPORT=otp

# HTTP server port
CHRONICLE_PORT=8080

# Number of competing consumers
CHRONICLE_CONSUMER_COUNT=3

# RabbitMQ Configuration (only used when CHRONICLE_TRANSPORT=rabbitmq)
RABBITMQ_HOST=localhost
RABBITMQ_PORT=5672
RABBITMQ_USER=guest
RABBITMQ_PASS=guest
RABBITMQ_VHOST=/
RABBITMQ_QUEUE=chronicle.events
