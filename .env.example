# Chronicle Configuration
# Copy this file to .env and modify as needed
# Load with: export $(cat .env | xargs)

# Transport backend: "otp" (default) or "rabbitmq"
CHRONICLE_TRANSPORT=otp

# Messaging Endpoint mode: "full" (default), "producer", or "consumer"
# - full: runs both HTTP server and consumers (default for development)
# - producer: HTTP server only, publishes events to channel
# - consumer: subscribes to channel, stores events (no HTTP server)
CHRONICLE_MODE=full

# HTTP server port (only used in producer or full mode)
CHRONICLE_PORT=8080

# Number of competing consumers (only used in consumer or full mode)
CHRONICLE_CONSUMER_COUNT=3

# RabbitMQ Configuration (only used when CHRONICLE_TRANSPORT=rabbitmq)
RABBITMQ_HOST=localhost
RABBITMQ_PORT=5672
RABBITMQ_USER=guest
RABBITMQ_PASS=guest
RABBITMQ_VHOST=/
RABBITMQ_QUEUE=chronicle.events
